on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # Setup key
      - run: set -eu
      - run: mkdir "$HOME/.ssh"
      - run: echo "$" > "$HOME/.ssh/key"
      - run: chmod 600 "$HOME/.ssh/key"
      # Deploy
      - run: rsync -e "ssh -p 1500 -i $HOME/.ssh/key -o StrictHostKeyChecking=no" --archive --compress --delete . u2145242@37.140.192.62:/var/www/u2145242/data/www/portal02-test.ru